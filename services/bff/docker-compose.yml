services:
  redis:
    image: redis:7
    ports:
      - "63792:6379"

  bff-gateway:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "8070:8070"
    depends_on:
      redis:
        condition: service_started
